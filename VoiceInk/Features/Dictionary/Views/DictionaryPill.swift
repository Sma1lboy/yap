        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

S        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

w        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

U        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

I        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

D        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

P        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

<        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

V        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

w        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

>        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

V        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

w        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

{        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

R        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

-        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

>        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

V        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

?        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

H        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

z        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

S        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

K        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

A        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

z        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

S        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

K        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

?        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

R        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

-        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

>        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

V        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

?        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

,        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

H        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

z        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

S        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

K        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

,        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

A        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

z        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

S        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

K        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

?        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

=        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

,        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

@        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

V        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

w        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

B        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

-        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

>        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

{        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

=        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

R        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

=        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

R        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

H        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

=        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

H        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

A        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

=        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

A        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

}        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

V        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

w        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

{        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

H        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

S        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

k        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

5        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

{        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

z        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

1        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

2        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

B        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

}        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

,        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

9        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

,        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

6        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

,        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

4        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

k        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

C        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

A        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

T        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

h        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

S        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

C        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

k        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

A        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

T        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

h        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

B        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

,        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

W        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

h        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

1        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

}        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

@        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

V        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

w        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

B        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

B        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

V        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

w        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

{        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

R        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

{        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

=        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

B        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

R        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

{        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

I        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

N        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

:        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

"        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

x        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

k        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

"        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

R        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

M        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

h        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

h        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

g        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

S        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

A        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

T        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

h        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

T        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

x        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

}        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

S        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

d        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

h        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

H        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

p        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

f        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

A        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

{        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

.        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

(        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

r        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

m        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

v        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

A        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

c        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

i        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

y        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

L        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

a        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

)        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

}        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

l        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

s        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

e        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

{        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

b        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

u        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

t        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

o        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

n        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

}        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

}        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

         HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

}        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

}        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }


        HStack(spacing: AppTheme.Spacing.x2) {
            label
                .font(AppTheme.font(.body))

            removeButton
        }
        .padding(.horizontal, AppTheme.Spacing.x2)
        .padding(.vertical, AppTheme.Spacing.x2)
        .background {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .fill(AppTheme.Surface.window.opacity(0.4))
        }
        .overlay {
            RoundedRectangle(cornerRadius: AppTheme.Radius.small)
                .stroke(AppTheme.Border.subtle, lineWidth: 1)
        }
        .shadow(color: Color.black.opacity(0.05), radius: 2, y: 1)
    }

